services:
  api:
    image: babyrecord-api:local
    pull_policy: build
    build:
      context: .
      dockerfile: server/Dockerfile

  web:
    image: babyrecord:local
    pull_policy: build
    build:
      context: .
      dockerfile: Dockerfile
